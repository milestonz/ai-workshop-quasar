rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 인증된 사용자는 모든 컬렉션에 접근 가능 (개발용)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 